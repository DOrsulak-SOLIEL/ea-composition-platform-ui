import {AlternateHrefPipe} from './alternate-href.pipe';

describe('alternate-href-pipe', () => {
  it('create an instance', () => {
    const pipe = new AlternateHrefPipe();
    expect(pipe).toBeTruthy();
  });
});
