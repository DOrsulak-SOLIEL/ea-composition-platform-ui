import {ExecuteProcessPipe} from './execute-process-pipe';

describe('OverflowPipe', () => {
  /*  it('create an instance', () => {
      const pipe = new ExecuteProcessPipe();
      expect(pipe).toBeTruthy();
    });*/
});
