import {SortHeaderNamePipe} from './sort-header-name-pipe';

describe('OverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new SortHeaderNamePipe();
    expect(pipe).toBeTruthy();
  });
});
